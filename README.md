# Claude API

Notebooks and examples for working with the [Anthropic Claude API](https://docs.anthropic.com/en/docs).

## Setup

Requires Python 3.14+ and [uv](https://docs.astral.sh/uv/).

```bash
uv sync
```

Create a `.env` file with your API key:

```
ANTHROPIC_API_KEY=your-api-key-here
```
